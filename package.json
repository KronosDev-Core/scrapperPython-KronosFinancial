{
  "name": "scrapper-kronosfinancial",
  "version": "1.1.6",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "rm -rf Drivers && pip install -q prisma selenium webdriver-manager",
    "generate": "prisma generate",
    "start": "pnpm install && python3 main.py"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^4.10.1"
  },
  "dependencies": {
    "@prisma/client": "^4.10.1"
  }
}
